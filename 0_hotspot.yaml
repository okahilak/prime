name: "Hotspot search"
description: "Hotspot search protocol: 500 open loop pulses"

safety:
  minimum_trial_interval: 2.0   # minimum seconds between consecutive trials

stages:
  - stage:
      name: "hotspot"
      trials: 500
